#include <stdio.h>
/**
 * main - main block
 * Description: print a to z but leave out q and e.
 * Return: 0
 */
int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
	  if (letter != "e" && letter != 'q') {
	        putchar(c);
		c++;
	  }
	}
	putchar('\n');

	return (0);
}
